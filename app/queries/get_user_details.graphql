query GetUserDetails($id: String!) {
  user(userId: $id) {
    id
    email
    displayName
    firstName
    lastName
    mobile
    avatar
    creationDate
    uuid
    groups {
      id
      displayName
    }
  }
}
